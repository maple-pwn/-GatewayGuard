# GatewayGuard PC 前端双版本改造方案

## 目标

当前 PC 端前端已经具备完整业务功能，包括：

- AI 助手
- 模拟控制台
- 事件中心
- 关于我们

现计划将首页以外的前端拆分为两套可切换的体验层：

1. 简洁版
当前已有后台工作台风格，保留现状作为稳定版本。

2. 炫酷版
新增一套与当前首页视觉风格一致的沉浸式前端。要求保持现有所有功能、接口和业务能力不缺失，同时加入动态特性和更强的展示效果。

此外，炫酷版中需要提供“切换至简洁版”按钮，允许用户随时回到当前工作台样式。

## 总体原则

### 1. 不改后端接口

本次改造只调整前端表现层与交互层，不修改后端 API，不新增后端依赖，不改变现有请求参数和返回结构。

继续复用：

- `/api/traffic/*`
- `/api/anomaly/*`
- `/api/llm/*`
- `/api/system/*`
- WebSocket 实时事件流

### 2. 双版本共用同一套业务逻辑

不能把“简洁版”和“炫酷版”做成两份完全独立的页面逻辑，否则后续维护成本会迅速失控。

正确做法：

- 视图层分为 `simple` 与 `immersive` 两套
- 数据请求、状态管理、格式化函数、轮询逻辑、WebSocket 逻辑统一抽到 composables/store 层
- 两套前端只复用不同的 UI 壳和组件编排方式

### 3. 首页视觉风格向内页延续

炫酷版要与当前首页保持统一风格：

- 全屏背景图或动态背景
- 大字号标题与沉浸式布局
- 深色、玻璃拟态、半透明、渐变遮罩
- 动态入场、轻微漂移、状态脉冲、图层视差

不能出现“首页像宣传页，内页又回到普通后台”的割裂感。

## 信息架构

建议把路由分为三层：

### 1. 落地首页

- `/`
- 独立展示页
- 无侧边栏
- 进入后可选择进入炫酷版或简洁版

首页按钮建议：

- `Start Immersive Console`
- `Enter Simple Console`

### 2. 简洁版工作台

保留现有工作台结构，统一挂到 `/simple/*`

建议路由：

- `/simple/assistant`
- `/simple/console`
- `/simple/events`
- `/simple/about`

特点：

- 保持当前左侧边栏布局
- 优先保证信息密度、可维护性、稳定性
- 作为保守模式和开发联调主模式

### 3. 炫酷版工作台

新增沉浸式工作台，统一挂到 `/immersive/*`

建议路由：

- `/immersive/assistant`
- `/immersive/console`
- `/immersive/events`
- `/immersive/about`

特点：

- 与首页风格统一
- 全屏背景、悬浮面板、动态状态层
- 保留全部业务能力
- 提供“切换至简洁版”按钮

## 版本切换设计

### 切换入口

炫酷版右上角固定一个按钮：

- `切换至简洁版`

简洁版也建议有对称按钮：

- `切换至炫酷版`

### 切换规则

同页面语义切换，不打断用户任务路径。

例如：

- `/immersive/console` -> `/simple/console`
- `/immersive/events` -> `/simple/events`
- `/simple/assistant` -> `/immersive/assistant`

### 状态记忆

前端保存用户偏好到 `localStorage`：

- `gg-ui-mode = simple | immersive`

用户从首页点击“开始”后，默认进入上次使用的版本。

## 页面映射方案

### 1. AI 助手

#### 简洁版

保留当前双栏模式：

- 左侧会话说明和推荐提问
- 右侧聊天主区域

#### 炫酷版

改为沉浸式分析舱：

- 背景为低透明动态流线/点阵
- 中央主聊天区
- 左侧悬浮“分析上下文卡”
- 右侧悬浮“系统状态卡”和“快速问题卡”
- 消息气泡加入淡入、滑入、打字加载动画

#### 功能保持

- 会话 ID
- 消息列表
- 推荐 prompt
- 发送消息
- LLM 返回
- 失败兜底提示

### 2. 模拟控制台

#### 简洁版

保留当前卡片分区：

- 指标总览
- 模拟控制
- 检测训练
- 采集控制
- 数据维护
- 实时告警流
- 最近流量记录

#### 炫酷版

改为“作战控制台”：

- 顶部为大屏状态条
- 左中为流量模拟与采集控制
- 右中为检测器状态与训练结果
- 底部为实时告警时间轴
- 右下角为最近流量滚动表
- 支持数字跳变动画、状态灯、扫描线、脉冲边框

#### 功能保持

- 模拟流量生成
- 检测器训练
- 异常检测
- 启停采集
- 导入抓包文件
- 清理数据
- 实时告警展示
- 最近流量展示

### 3. 事件中心

#### 简洁版

保持现有“列表 + 详情 + AI 分析”结构。

#### 炫酷版

改为“事件指挥面板”：

- 左侧高风险事件队列
- 中央事件详情主面板
- 右侧 AI 研判摘要
- 顶部风险计数与过滤标签
- 支持事件卡片 hover 发光、严重度颜色脉冲、详情切换过渡动画

#### 功能保持

- 事件列表
- 分级筛选
- 事件详情
- AI 分析/报告
- 批量操作能力

### 4. 关于我们

#### 简洁版

保留当前说明文档式布局。

#### 炫酷版

改为“系统态势总览页”：

- 系统状态大卡
- LLM provider/model 状态
- 前端版本切换说明
- 平台定位介绍
- 带动态背景与信息浮层

## 组件分层方案

建议新增以下目录结构：

```text
frontend/src/
  layouts/
    SimpleLayout.vue
    ImmersiveLayout.vue
  views/
    simple/
      AssistantView.vue
      ConsoleView.vue
      EventsView.vue
      AboutView.vue
    immersive/
      AssistantView.vue
      ConsoleView.vue
      EventsView.vue
      AboutView.vue
  components/
    shared/
    simple/
    immersive/
  composables/
    useTrafficOverview.js
    useRealtimeAlerts.js
    useChatSession.js
    useSystemStatus.js
    useUiMode.js
```

### 布局职责

#### `SimpleLayout.vue`

- 复用当前侧边栏
- 复用当前工作区页头
- 适配 `simple/*` 路由

#### `ImmersiveLayout.vue`

- 全屏背景
- 顶部轻量导航/右上角模式切换
- 内页浮层式容器
- 炫酷版页面统一视觉容器

### composables 职责

#### `useChatSession.js`

- 会话 ID 管理
- 消息发送
- loading 状态
- 滚动到底部

#### `useTrafficOverview.js`

- stats 获取
- collect 状态
- train/detect 状态
- 近期流量

#### `useRealtimeAlerts.js`

- WebSocket 建连
- 实时告警数组
- 断线重连

#### `useSystemStatus.js`

- 系统状态获取
- LLM provider/model 显示

#### `useUiMode.js`

- 当前模式读取
- 模式切换
- 路由映射

## 动态特性设计

炫酷版必须加入动态性，但动态不能影响功能效率。

### 背景动态

- 首页同风格全屏背景
- 轻微 Ken Burns 式背景位移动画
- 低透明雷达环、网格流线或粒子漂浮
- 深色渐变遮罩保持可读性

### 面板动态

- 卡片 hover 上浮
- 边框光晕呼吸
- 严重事件红色脉冲
- 指标数字滚动增长动画
- 列表切换使用淡入/位移动画

### 数据动态

- 告警流新数据插入时高亮
- WebSocket 到新告警时触发局部闪烁
- 模型训练中显示扫描条/进度呼吸动画
- 聊天回复中显示打字指示器

### 页面转场

- 简洁版与炫酷版切换使用淡出淡入
- 炫酷版页面切换使用短距离滑入
- 避免重动画导致响应迟滞

## 视觉规范

### 颜色

沿用首页的深色基调，建议扩展：

- 背景主色：深蓝黑、深青黑
- 强调色：冷蓝、青绿
- 风险色：橙、红
- 中性文字：半透明白、浅灰蓝

### 材质

- 玻璃拟态面板
- 透明描边
- 模糊背景
- 柔和阴影，不做廉价霓虹泛滥

### 字体和排版

- 大标题保持首页同一风格
- 业务页正文仍以可读性优先
- 标题与数据卡允许更强视觉对比

## 接口与功能保持清单

炫酷版必须逐项覆盖当前能力：

### Chat

- 输入
- 发送
- 会话 ID
- 推荐问题
- 消息展示
- loading
- 异常兜底

### Console

- stats
- simulate
- train
- detect
- collect start/stop
- import file
- clear data
- partial clean
- recent packets
- realtime alerts

### Events

- events list
- filters
- detail
- AI analyze/report
- batch operations

### About

- system status
- LLM provider
- LLM model
- detector status
- 产品说明

## 实施步骤

### 第一阶段：结构拆分

1. 把当前工作台重命名为简洁版布局
2. 路由拆分为 `/simple/*` 与 `/immersive/*`
3. 首页按钮接入双入口
4. 新增 UI 模式切换逻辑

### 第二阶段：逻辑抽离

1. 将 Chat、Console、Events、About 中的数据请求与状态抽到 composables
2. 保证简洁版继续可用
3. 做最小回归验证

### 第三阶段：炫酷版实现

1. 先完成 `ImmersiveLayout`
2. 再做 `console` 炫酷版，因为它最能建立整体风格
3. 再完成 `assistant`
4. 再完成 `events`
5. 最后完成 `about`

### 第四阶段：动效与联调

1. 加入过渡动画
2. 加入实时数据高亮
3. 检查性能与可读性
4. 检查不同分辨率表现

## 风险与约束

### 1. 不能复制粘贴两套业务代码

如果直接复制当前页面再改样式，后续任何接口变更都要维护两份逻辑，会很快失控。

### 2. 炫酷不能牺牲操作效率

控制台和事件中心本质上仍是操作型界面，不是纯展示页。动画只能增强理解，不能拖慢操作。

### 3. 需要控制包体增长

当前前端构建包已经较大。炫酷版增加动画和新组件时，应避免再引入重量级动画库。

建议优先：

- CSS 动画
- Vue 内建 transition
- 少量 ECharts 动态配置

不建议：

- 大型 3D 引擎
- 重粒子库
- 复杂 WebGL 依赖

## 建议交付标准

完成后应满足：

1. 首页可进入两种版本
2. 炫酷版和简洁版都能完整覆盖现有功能
3. 炫酷版有明确风格一致性，不是普通后台换皮
4. 两种版本能一键切换且保持当前页面语义
5. 所有现有接口保持不变
6. 桌面端主要分辨率下无明显布局错乱

## 推荐下一步

建议按以下顺序实际开发：

1. 先重构路由和布局层
2. 再抽离共享业务逻辑
3. 先做炫酷版控制台
4. 再把同风格推广到助手、事件中心、关于页

这样风险最低，也最容易持续验收。

## 关于我们页面专项改造方案

### 改造目标

当前“关于我们”页更像技术说明页，信息完整，但亮点表达不够强。

新的目标是：

- 简洁版继续保留基础说明能力
- 正式版把“关于我们”升级为项目价值展示页
- 重点突出项目优点、创新点、评测结果和可信度
- 视觉上与首页、正式版整体风格一致

### 数据依据

页面内容应优先来自：

- `GatewayGuard/reports/dataset_survey_and_evaluation.md`

当前可直接提炼为对外展示的核心事实：

- 发布版纳入 `4` 个开源 CAN 数据集
- 共覆盖 `15` 个 case，其中攻击 case `11` 个
- 攻击 case 加权 `weighted_f1 = 0.9617`
- `4` 个正常流量数据集未出现新增回归
- `ML` 正常段误报接近零，仅 `B-CAN normal_eval` 保留 `5` 条 `ML` 告警，对应 `FPR = 0.0001`
- `Car-Hacking dos_tail`、`CAN-FD flooding` 的 `F1 = 1.0000`
- `CAN-FD fuzzing` 的 `F1 = 0.9988`
- 本轮最重要的改进是将 `B-CAN ddos_tail`、`CAN-FD malfunction`、`Car-Hacking rpm_tail`、`Car-Hacking gear_tail` 从接近不可用拉回到可提交水平

### 页面定位

“关于我们”页不再只回答“这个项目是什么”，而是回答以下四个问题：

1. 我们解决了什么问题
2. 我们的方法相比传统做法强在哪里
3. 我们的结果是否可信
4. 这个平台为什么值得继续使用或展示

### 正式版页面结构

建议把正式版 `/immersive/about` 改造成 5 个视觉分区。

#### 1. Hero 区

用途：

- 用一句话定义项目价值
- 建立项目气质，而不是先堆系统状态

建议文案方向：

- `GatewayGuard is a vehicle security analysis platform built for multi-source traffic, anomaly detection, and explainable defense workflows.`

中文可落地为：

- `GatewayGuard 是一个面向智能网联汽车场景的安全分析平台，聚合多源流量采集、异常检测与可解释研判能力。`

视觉建议：

- 延续首页大背景
- 左侧大标题和一句副标题
- 右侧悬浮“能力标签”
- 弱化传统卡片感，突出发布页气质

#### 2. 项目优势区

用途：

- 直接告诉用户“这个项目比普通演示系统强在哪里”

建议展示为 4 张优势卡：

1. 多源接入
支持 `CAN / ETH / V2X / PCAP / 模拟器`

2. 规则 + ML 协同
不是单一规则库，也不是纯黑盒模型，而是联合检测策略

3. 低误报控制
正常流量 `ML FPR` 接近零，体现上线可用性

4. 研判闭环
检测、事件、AI 分析、报告输出在一个平台内闭合

视觉建议：

- 四张横向能力卡
- 每张卡带微动效和悬浮高亮
- 用图标、关键词、短说明三层表达

#### 3. 创新区

用途：

- 突出“创新点”，避免页面只剩性能数字

建议提炼 4 个创新点：

1. 数据集迁移能力
同一平台在多个开源数据集上做独立训练与评测，不绑定单一数据源。

2. 发布级评测口径
明确区分纳入和排除项，不混用本地样例和开放集结果，口径更干净。

3. 薄弱场景修复
不是只展示最好看的 case，而是把原本接近不可用的场景拉回到可交付水平。

4. AI 解释层补全
不仅检测异常，还能在事件页和助手页提供解释、归因和处置建议。

视觉建议：

- 使用时间轴或分层信息带
- 每个创新点配一条“问题 -> 改进 -> 结果”
- 可加入扫描线、编号标记、渐变分隔

#### 4. 评测结果区

用途：

- 用高质量数据建立可信度

建议顶部放 4 个核心结果卡：

- `4` 个开源数据集
- `11` 个有效攻击 case
- `weighted_f1 = 0.9617`
- `normal ML FPR ≈ 0`

然后下方补两个模块：

1. 最强表现

- `Car-Hacking dos_tail: F1 = 1.0000`
- `CAN-FD flooding: F1 = 1.0000`
- `CAN-FD fuzzing: F1 = 0.9988`

2. 关键修复

- `B-CAN ddos_tail`
- `CAN-FD malfunction`
- `Car-Hacking rpm_tail`
- `Car-Hacking gear_tail`

建议表达方式：

- 左侧数字大卡
- 右侧“表现最强 / 关键修复”双列信息块
- 数据卡加入跳字动画，但节奏要克制

#### 5. 项目价值总结区

用途：

- 收束页面逻辑，避免内容只是信息堆砌

建议总结方向：

- 不是单点检测 demo，而是完整的车载网络安全分析工作台
- 不只强调高分 case，也强调误报控制和弱项修复
- 更适合课程展示、答辩展示、系统演示和进一步工程扩展

建议做成一张大底卡：

- 左侧总结文字
- 右侧两个按钮
- `查看正式版控制台`
- `切换至简洁版`

### 简洁版页面建议

简洁版 `/simple/about` 不需要大改视觉，但应该改内容重心。

建议结构：

- 顶部项目简介
- 中部优势列表
- 下部评测摘要

简洁版也应加入以下信息，而不是只保留系统状态：

- `weighted_f1 = 0.9617`
- `11` 个攻击 case 有效
- 正常流量无新增回归
- 低误报
- 多数据集覆盖

### 页面语气建议

避免“宣传口号式空话”，要做到：

- 每个优点都能落到具体机制
- 每个创新点都能落到评测或流程
- 每个性能结论都有明确数据支撑

不建议写法：

- “我们系统性能优秀”
- “创新性很强”

建议写法：

- “发布版在 4 个开源 CAN 数据集、11 个攻击 case 上达到 weighted F1 = 0.9617，同时正常流量未引入新增回归。”

### 视觉风格建议

正式版“关于我们”应比当前版本更像“成果展示页”：

- 背景延续首页深色主视觉
- 模块边界用玻璃拟态和发光描边
- 数据区可用数字翻牌或轻量动效
- 创新区可用编号轨道或技术剖面图样式
- 不使用过多表格，优先用卡片和可扫描结构

### 图表展示增强建议

仅靠文字和数字卡，还不足以把“项目效果”和“可信度”讲清楚。

正式版“关于我们”页建议加入直角坐标图和性能图表，用来强化以下信息：

- 提升幅度有多大
- 哪些 case 表现最强
- 哪些 case 仍然偏难
- 项目不是只展示单一最佳结果，而是有分布、有修复、有对比

建议优先加入以下 4 类图表。

#### 1. 攻击 case F1 柱状图

用途：

- 展示不同攻击 case 的效果分布
- 一眼看出哪些 case 达到接近满分，哪些 case 仍然相对偏弱

推荐数据：

- `Car-Hacking dos_tail: 1.0000`
- `CAN-FD flooding: 1.0000`
- `CAN-FD fuzzing: 0.9988`
- `Car-Hacking fuzzy_tail: 0.9692`
- `Car-Hacking rpm_tail: 0.9553`
- `B-CAN ddos_tail: 0.9473`
- `B-CAN fuzzing_tail: 0.9138`
- `M-CAN fuzzing_tail: 0.9135`
- `Car-Hacking gear_tail: 0.8962`
- `CAN-FD malfunction: 0.8955`

展示方式：

- 横轴：攻击 case
- 纵轴：F1
- 柱体颜色按风险或表现分级
- `F1 = 1.0000` 的 case 可用更亮强调色

#### 2. Precision-Recall 直角坐标散点图

用途：

- 这是最值得加的一张“直角坐标图”
- 用来展示各攻击场景在 `Precision` 与 `Recall` 上的整体分布
- 比单一 F1 更能体现“高分是靠高 precision 还是高 recall 得来的”

推荐设计：

- 横轴：Recall
- 纵轴：Precision
- 每个点代表一个攻击 case
- 点的大小可表示样本规模或攻击样本数
- 点的颜色可表示数据集来源

适合传达的信息：

- 哪些点位于右上角，表示“又准又全”
- 哪些点虽 precision 高但 recall 稍弱，说明仍有漏报空间
- 哪些点经过修复后从边缘区域回到可用区

建议重点标注的点：

- `Car-Hacking dos_tail`
- `CAN-FD flooding`
- `CAN-FD fuzzing`
- `CAN-FD malfunction`
- `Car-Hacking gear_tail`

#### 3. 正常流量稳定性图

用途：

- 证明项目不是只会“报很多”，而是误报控制也稳定

推荐数据：

- `4` 个正常流量数据集无新增回归
- `B-CAN normal_eval` 的 `ML FPR = 0.0001`
- 其余正常段 `ML FPR = 0`

推荐图形：

- 小型柱状图
- 或极简横条图

横轴：

- 数据集名称

纵轴：

- `ML false_positive_rate`

这张图的意义在于强调：

- 项目不是只追求攻击检测高分
- 也在控制正常流量上的误报

### 页面中的图表布局建议

正式版“关于我们”页建议这样放图：

#### 上半区

- 左侧：项目优势卡 / 创新点卡
- 右侧：`weighted_f1`、数据集数量、攻击 case 数量等核心数字卡

#### 中部主图区

- 左：攻击 case `F1` 柱状图
- 右：`Precision-Recall` 直角坐标散点图

这是整页最重要的视觉中心。

#### 下半区

- 正常流量稳定性图

这样布局的好处是：

- 先讲价值
- 再讲能力
- 最后讲证据

### 图表风格建议

由于正式版整体是深色沉浸式风格，图表也应统一：

- 使用深色背景透明图层
- 坐标轴和网格线弱化
- 数据点和柱体用冷蓝、青绿、橙红区分
- hover 显示精确数值
- 动画只做首次入场，不做持续晃动

避免：

- 花哨但难读的 3D 图
- 过度饱和颜色
- 同时放太多小图导致页面碎裂

### 推荐优先级

如果时间有限，优先做这两张：

1. 攻击 case F1 柱状图
2. Precision-Recall 直角坐标散点图

原因：

- 一张讲总体效果
- 一张讲效果分布
- 两张图就足以把“项目优点 + 可信度 + 创新价值”同时拉起来

### 与传统方案对比图建议

在“关于我们”页中加入“与传统方案对比”的内容，会明显增强说服力。

但这里必须区分两类对比：

#### 1. 性能对比

适合前提：

- 手里有可复现实验结果
- 对比对象、数据集、训练方式、评测口径一致

可比维度：

- `F1`
- `Precision`
- `Recall`
- `False Positive Rate`

适合图形：

- 分组柱状图
- 多指标对比表
- 小型雷达图

注意：

如果当前没有完整、统一口径的传统基线实验，不建议直接做“性能碾压图”，否则容易削弱可信度。

#### 2. 能力对比

这是当前更稳妥、也更适合展示页的方案。

不强调“绝对性能谁高谁低”，而是强调系统能力覆盖差异。

建议对比对象：

- 传统规则方案
- 纯机器学习检测方案
- GatewayGuard

建议对比维度：

- 多源接入能力
- 低误报控制
- 跨数据集适配
- 薄弱场景修复能力
- AI 解释能力
- 报告闭环能力
- 系统化展示能力

### 推荐图表 1：能力对比雷达图

用途：

- 展示 GatewayGuard 不是单点检测模型，而是完整平台

建议维度：

- Multi-source Access
- Detection Robustness
- Low False Positives
- Cross-dataset Generalization
- Explainability
- Reporting Workflow

对比系列：

- Rule-based
- Pure ML
- GatewayGuard

表达重点：

- 传统规则方案在解释性上可能不差，但跨数据集适配和复杂场景泛化较弱
- 纯 ML 方案在检测能力上较强，但解释层与报告闭环不足
- GatewayGuard 在能力覆盖上更均衡

### 推荐图表 2：能力对比表

如果雷达图太抽象，可再补一张更落地的对比表。

推荐形式：

| 能力 | 传统规则方案 | 纯 ML 方案 | GatewayGuard |
| --- | --- | --- | --- |
| 多源流量接入 | 弱 | 中 | 强 |
| 误报控制 | 中 | 中 | 强 |
| 跨数据集适配 | 弱 | 中 | 强 |
| 异常解释 | 弱 | 弱 | 强 |
| 事件报告 | 无 | 无 | 强 |
| 展示完整性 | 弱 | 弱 | 强 |

这张表的价值在于：

- 读者不需要理解全部算法细节，也能看懂系统层面的优势

### 推荐图表 3：检测闭环流程对比图

这类图比性能图更容易体现“创新点”。

推荐做成左右对照：

#### 传统方案

- 流量采集
- 单点检测
- 告警输出

#### GatewayGuard

- 流量采集
- 规则 + ML 协同检测
- 事件中心聚合
- AI 语义分析
- 报告输出与处置建议

这张图适合传达：

- 项目创新不只是模型分数
- 更在于从检测到解释、从告警到报告的完整闭环

### 页面中的摆放建议

“传统方案对比”建议放在“项目优势区”和“评测结果区”之间。

原因：

- 先告诉用户我们比传统方案多做了什么
- 再用评测结果证明这些扩展不是空壳

建议布局：

- 左侧：能力对比雷达图
- 右侧：检测闭环流程图
- 下方：如有正式基线结果，再补性能对比柱状图

### 推荐口径

如果当前没有完整传统基线实验结果，页面上应明确采用：

- “能力对比”
- “流程对比”

而不是声称：

- “全面优于所有传统方案”

建议文案方向：

- `相比传统单规则或纯模型检测流程，GatewayGuard 更强调多源接入、低误报控制、跨场景适配和 AI 解释闭环。`

这样既保守，也更专业。

### 最终页面应传达的三层信息

第一层：
这是一个面向车载网络安全的完整平台，而不是单一算法页面。

第二层：
它在多个公开数据集和攻击场景上具备稳定、可解释、可展示的性能。

第三层：
它不仅有检测结果，还有低误报控制、薄弱场景修复和 AI 解释层，是一个更完整的系统方案。
